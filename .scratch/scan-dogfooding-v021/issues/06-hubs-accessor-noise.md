# Issue 06 — hubs 桶 getter/setter 占榜，change-impact 信号失真

> *This was generated by AI during triage.*

Status: enhancement / needs-triage

## 现象（复现仓库：spring-petclinic-microservices，repoId `repo-d44585d8-4b31-49e8-8fe4-7a7b05595198`）

hubs 桶 top10 中：

- `Vet.getSpecialtiesInternal`（getter，PageRank 0.0136 榜首）；
- `Visit.setPetId`（纯 setter，0.0085）；
- `Owner.getPetsInternal`、`OwnerDetails.pets`、`AIDataProvider.getCustomerServiceUri` 等访问器/DTO 方法。

hubs 的卖点是 "Change-impact hubs (highest PageRank)" + nextAction "Run codecompass_refactor_plan on any of these before touching them"——但把改动影响面聚焦到 getter/setter 上，引导价值接近零，还挤掉了真正的结构枢纽。

## 为什么 needs-triage

- 过滤策略是设计判断：按命名启发式（`get*/set*/is*`）过滤简单可落地但有误伤面（`getOrCreateX` 类有真实逻辑）；按"方法体行数 <N 的 accessor"过滤更稳但阈值拍脑袋。推荐组合：**命名启发式 + 行数上限（如 ≤5 行）**双条件才剔除。
- 与 issue 04（注解感知）同属"信号提纯"主题，可考虑合并进同一个 v0.22 增量一起设计（milestone 归属需 maintainer 定）。
- 本仓库与 lazygit 的 hubs 榜单受此影响较小（lazygit 的问题主要是 vendor，见 issue 01），优先级低于 01/02/03。

## 验收（方向定后补全）

- petclinic 重跑 scan：hubs top10 无纯 accessor；既有 golden eval（route-chain/architecture 阈值）不回退。
