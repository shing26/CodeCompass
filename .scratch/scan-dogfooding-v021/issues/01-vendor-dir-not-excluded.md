# Issue 01 — vendor/ 目录未排除，hubs/oversized/oversizedFiles 三桶被污染

> *This was generated by AI during triage.*

Status: bug / ready-for-agent

## 现象（复现仓库：jesseduffield/lazygit，repoId `repo-609a7d6d-c824-4e93-81ef-fa52f3442af4`）

index_repo 把 `vendor/` 全量索引进来并参与全部五桶计算。仓库内 `.go` 文件 1952 个，其中 vendor 占 856（44%）。

scan 结果（v0.21.0，2026-09-06）：

- **hubs** top10 中 3 个是 vendor 符号（`vendor/github.com/integrii/flaggy/subCommand.go`、`vendor/github.com/mailru/easyjson/buffer/pool.go`、`vendor/github.com/lucasb-eyer/go-colorful/colors.go` 两个方法）；
- **oversized** top10 中 8 个是 vendor（emoji_codemap.go 4800 行、x/text tables 等）；
- **oversizedFiles** top10 **全部**是 vendor 文件——真实业务文件的债务热点被完全挤出榜单。

对 scan 的核心场景（agent 找"该动哪里"）来说，vendor 噪声让 lazygit 的三个桶输出基本不可用。

## 期望

vendor / 第三方与生成代码不应进入 scan 候选。同理适用的排除面：`node_modules/`（TS）、`target/`（Java/Maven）、`dist/`。

## 改动点（建议，agent 可自行核实细节）

- 排除的**层级**需要先定：索引层（`repoqa-parser.ts` / index_repo 文件枚举时跳过）vs scan 层（scan-engine 桶计算时过滤）。推荐索引层——同时让 hubs/deepChains/radar 等所有下游受益，且省 token/存储；但要注意索引层排除会让这些路径在 trace/diagnose 里也消失（对第三方依赖来说这正是期望行为）。
- 排除清单建议集中在单一常量/配置处（参考现有 `isTestPath` 的位置模式），同步补 `repoqa-scan.test.ts` / `scan-engine.test.ts` fixture：vendor 下的超大表函数不再进入 oversized 榜单。
- Java 侧注意 `**/generated/**` 一类生成目录是否一并处理可留给 agent 判断，但 vendor/node_modules/target 三项必须在。

## 验收

- 对 lazygit 重跑 scan（重新 index 后）：oversizedFiles top10 无 vendor 路径；hubs top10 无 vendor 符号；orphanedPublic total 显著下降（vendor 符号不再计入）。
- e2e 全绿；单测覆盖排除规则。
