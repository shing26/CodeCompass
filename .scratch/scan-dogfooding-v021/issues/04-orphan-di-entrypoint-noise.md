# Issue 04 — 孤儿桶 DI/入口点假阳性主导，nextAction 引导危险

> *This was generated by AI during triage.*

Status: closed — fixed in v0.23.0 (commit 4df2613), verified 2026-09-08 by Persona-QA triage

## 现象（复现仓库：spring-petclinic-microservices，repoId `repo-d44585d8-4b31-49e8-8fe4-7a7b05595198`）

孤儿桶 total 184/595 ≈ 31%，top10 中约 9 个是 DI/入口模式假阳性：

- `@Bean` 工厂方法：`ApiGatewayApplication.loadBalancedRestTemplate`、`routerFunction`、`defaultCustomizer`；
- `@FeignClient` 接口：`CustomersServiceClient`、`VisitsServiceClient`（被 Spring 注入使用，静态零调用是**正常形态**）；
- 入口点：`*Application` 类 + `main` 方法（lazygit 的 `main.go:15 main` 同理）。

现有 note（"reflective lookups, dynamic proxies … invisible to AST analysis"）免责方向正确，但同桶的 nextAction 仍指向 "Plan a safe teardown with codecompass_module_evolution (DEPRECATE)"——在注解密集仓库里这是**危险引导**：agent 照做会去拆 Spring 装配面。note 与 nextAction 相互矛盾。

## 为什么 needs-triage（不直接 ready-for-agent）

涉及设计判断，需要 maintainer 定方向：

1. **策略选择**：注解感知降权（识别 `@Bean`/`@FeignClient`/`@SpringBootApplication`/`@EventListener`/Go `main` 等，标记为 "externally wired" 并从孤儿桶剔除或单列）vs 仅改 nextAction 文案（便宜但治标）。推荐前者——单列一个 "externally wired" 语义既消灭假阳性又保留信息。
2. **范围**：只做 Spring/Go 入口点，还是建立注解白名单常量供多语言复用。
3. 若单列：payload 契约变化（`ScanBucket` 新 id 或 candidate 新 flag）属于共享区改动，按并行协作约定需先落 spec。

## 已确认的事实（供决策）

- JavaAdapter 已能识别 route（`@RestController` 方法→route kind，已在孤儿桶排除之列，见 `scan-engine.ts:90` 注释）——同样的注解识别机制可扩展到 `@Bean` 等，工程上顺路。
- lazygit `main`、petclinic `main` 都进孤儿桶；入口点排除在 Go 侧可与 issue 02 的修复独立进行。

## 验收（方向定后补全）

- petclinic 重跑 scan：`@Bean`/`@FeignClient`/`*Application.main` 不再作为"待拆除"候选出现，或以明确语义单列；nextAction 与 note 不再矛盾。

## Comments

- 2026-09-08 Persona-QA triage：v0.23.0（commit `4df2613`）已落地推荐方向——`scan-engine.ts:40-41`（wiredExcluded 桶文案）、`:75-88`（wiredKindOf：@Bean/@FeignClient/@Configuration 成员归 'di'，parentType 传播）。遗留说明：petclinic 动态复跑未做，机制以代码+单测为准。已并入 triage-backlog（D:/CodeCompass/.scratch/chat-merge/qa/triage-backlog.md）closed 表。
