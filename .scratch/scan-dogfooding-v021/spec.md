# Spec: codecompass_scan 五桶 dogfooding 回访（v0.21.0 后取证）

> *This was generated by AI during triage.*

- 日期：2026-09-06
- 性质：取证任务（非构建），源自 handoff `C:/Users/Shing/AppData/Local/Temp/codecompass-handoff-dogfooding.md`
- 目标：五桶（orphanedPublic / hubs / oversized / deepChains / oversizedFiles）此前只在合成 fixture 上验证过；本回访用真实仓库测误报率、阈值合理性与 nextAction 引导效果。

## 环境健康（HANDOFF §6 清单，全绿）

- typecheck 零错误；控制面单测 546/546；前端 vitest 全绿；build 成功；e2e 52/52（含 golden eval 97 题全阈值通过）。
- master = tag = 远端 = `368a973`（v0.21.0）。

## 样本仓库（index_repo → list_repos 轮询 → scan 全链）

| 仓库 | 语言 | fileCount / symbolCount | 备注 |
|---|---|---|---|
| jesseduffield/lazygit | Go | 2349 / 45423 | 中型单语言；vendor/ 在仓库内 |
| spring-petclinic/spring-petclinic-microservices | Java | 206 / 595 | 多模块 + Spring DI 容器（反射重灾区） |
| CodeCompass 本仓库 | TS/Python | 577 / 1406 | 基线对照 |

index_repo 异步契约（ADR-0016）全链顺畅：两个远端仓库 clone+index 数十秒内 ready，无需人工干预。

## 五桶观察汇总

### 孤儿桶（orphanedPublic）——最大问题区

| 仓库 | total / 符号总数 | 假阳性占比（top10 抽样） | 主因 |
|---|---|---|---|
| lazygit | 19629 / 45423 ≈ **43%** | ~10/10 | Go 调用边几乎全标 dynamic（issue 02）+ vendor 全量入索引（issue 01） |
| petclinic | 184 / 595 ≈ **31%** | ~9/10 | DI/入口点模式（issue 04） |
| 本仓库 | 1166 / 1406 ≈ **83%** | ~8/10 | 测试夹具符号未被排除（issue 03） |

硬实锤（复现于克隆本地 `C:/Users/Shing/.mhw/clones/lazygit-1788629449744`）：包级函数 `Run`（`pkg/app/app.go:41`）在 `pkg/app/entry_point.go:177` 有真实静态调用点，scan 仍报 "0 static callers"。根因见 issue 02。

### hubs 桶

- petclinic：getter/setter 上榜（`Vet.getSpecialtiesInternal`、`Visit.setPetId`）——PageRank 未过滤 accessors（issue 06）。
- lazygit：top10 一半是 vendor 符号（issue 01）。

### oversized / oversizedFiles 桶

- 阈值本身合理：本仓库命中（`http.ts` 984 行、`repoqa-http.test.ts` 3094 行）符合直觉；petclinic 0 命中符合其小方法风格。
- lazygit top10 全是 vendor 生成表文件（emoji codemap 4800 行等）——信噪比问题归 issue 01，非阈值问题。

### deepChains 桶

- lazygit total=0（无 HTTP route 识别，可接受），但空桶 nextAction 仍引导 "Run codecompass_diagnose on an entry"——无 entry 可指（issue 05）。
- 本仓库最深仅 depth 2 且混入 depth-1 平凡项；petclinic 出现 `createOwner → map → setAddress` 这类 setter 链——链价值有限，暂记录不立项（accessor 过滤落地后自动缓解）。

### nextAction 引导

- 危险引导：petclinic 孤儿桶（DI 假阳性主导）的 nextAction 指向 DEPRECATE 拆除；note 虽有免责声明，但引导与 note 相互矛盾（issue 04 一并评估）。
- 空桶引导失效：issue 05。

## 产出 issues

| # | 标题 | category | state |
|---|---|---|---|
| 01 | vendor/ 目录未排除，三桶被污染 | bug | ready-for-agent |
| 02 | Go 包级/跨包调用边被标 dynamic，孤儿桶 43% 假阳性 | bug | ready-for-agent |
| 03 | isTestPath 不认 `*.test.tsx` / `*_test.go`，测试符号进孤儿桶 | bug | ready-for-agent |
| 04 | 孤儿桶 DI/入口点假阳性 + DEPRECATE 危险引导 | enhancement | needs-triage |
| 05 | 空桶 nextAction 引导失效 | bug | ready-for-agent |
| 06 | hubs 桶 getter/setter 占榜 | enhancement | needs-triage |

## 正面观察（保持不动）

- oversized/oversizedFiles 阈值在真实仓库命中合理；oversized 桶的 note（"复杂度代理，非质量度量"）表述准确。
- 异步索引契约（ADR-0016）在远端仓库上体验良好；cockpitDeepLink 正常生成。
- scan 对 45k 符号仓库的响应在可接受范围。

## 边界

- 本 spec 只取证与分流，不实现。修复走 issues；版本预占按 `docs/agents/parallel-collaboration.md`（动手前查 CHANGELOG，下一个可用 minor 大概率 0.22.0）。

---

## v0.22.0 立项定稿（2026-09-06 grill 访谈结论）

访谈定案（grill-with-docs，4 问 3 答 + 1 默认）：

1. **范围**：01+02+03+05 四项全做 → v0.22.0（用户选定"四项全做"）。
2. **issue 02 深度**：最小修（裸调用 dynamic:false）+ 增强修（import 映射跨包 pkg.Func 解析）都做（用户选定）。
3. **issue 01 层级**：索引层排除（index_repo 文件枚举跳过 vendor/node_modules/target），全部下游受益（用户选定）。
4. **issue 05 修法**：**用户未作答**——按推荐默认执行（v0.6 先例）：nextAction 字段恒存在、空桶改中性文案，零契约变更。

04/06 两个 needs-triage 项不入本版，留待后续增量（04 的单列语义是契约改动，需先落 spec）。
