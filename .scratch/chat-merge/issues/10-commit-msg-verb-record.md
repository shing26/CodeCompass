# 10：批1 commit 标题动词误差（POST vs GET）——记录在案

> *This was generated by AI during triage.*

Status: wontfix
标签：记录 / 无代码动作 / 来源：v0.25.0 双轴 review + QA 报告 QA-06 附带项

## 决议

批次 1 提交 `cff344d` 标题写 "POST /api/dialog/folder"，实现与前端客户端均为 **GET**。代码与文档（HANDOFF/CONTEXT/CHANGELOG）自洽，仅 commit 标题有误。已推送历史不改写（rebase 会毁掉 tag `v0.25.0` 与 CI 追溯）；后续引用该 commit 时以 GET 为准。本票仅作记录，close。
