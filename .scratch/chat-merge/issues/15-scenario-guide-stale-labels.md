# 15：首屏引导文案滞后于 tab 改名与真实控件（QA-06）

> *This was generated by AI during triage.*

Status: closed
标签：bug / P3（体验晦涩） / 来源：v0.25.0 QA 定点回归；8ac9bea 轮改名漏网

## Agent Brief

**Category:** bug
**Summary:** 空态引导/场景导览文案仍指称已不存在的 UI 控件名——小白照文案操作会在桌面端找不到入口。

**Current behavior:**
引导文案写「顶部「+」按钮」「「架构指标」」「「智能体对话」」。真实 UI：桌面端导入入口是「Import repo」（「+」仅窄屏）；tab 现为六枚新名（代码拓扑/架构仪表盘/架构问答/变更审计/Diff 影响面/规范演进）。

**Desired behavior:**
所有面向用户的引导/空态/提示文案引用**当前真实控件名**，并对平台相关入口标注形态（「顶部 Import repo 按钮（窄屏为「+」）」）。

**Key interfaces:**
- 承载空态引导/ScenarioGuide 文案的组件常量或内联串 — 逐条更名
- 若引导文案集中为常量表，则一并核对 tab 新名的 tooltip 措辞一致性

**Acceptance criteria:**
- [x] 全前端源码 grep「架构指标」「智能体对话」零命中（CHANGELOG/CONTEXT 等历史记述文档除外，不算 UI 文案）
- [x] 引导内提及导入入口时桌面/窄屏两形态均有交代
- [x] web 测试中如有断言旧文案的用例，随文案一并更新（这是新票合法改断言，不违反 v0.25 归档口径）
- [x] 375px 与 1280px 两视口人工/截图核对引导可读（自动化断言文案存在即可）

**Out of scope:**
- 问答/演进入口**心智分离**的文案整体重设计——那是 v0.26 预留 A（见 issues/07 排期节），本票只做「旧名 → 新名」的失实修正，不重排引导结构

## Comments

- 实现（2026-09-10）：失实文案唯二落点均在 `Canvas.tsx` 空态引导（全前端 grep 核实，无别的命中）。① 「顶部「+」按钮」→「顶部『Import repo』按钮（窄屏为「+」）」（与 TopBar `sm:` 断点实况一致）；② 旧 tab 名换六枚现行名——「架构指标」→「架构仪表盘」、「智能体对话」→「架构问答」，其余半句改写为「代码拓扑/变更审计/Diff 影响面/规范演进」实名；③「在对话页输入」→「在『架构问答』输入」（同类指称失实，顺手收敛）。ScenarioGuide 本身为纯渲染组件，steps 文案（变更审计/Diff/演进三视图）逐一核对无旧控件名引用。
- 断言：无既有用例断言旧文案（无需改断言）；`Canvas.test.tsx` 新增 ticket-15 用例——新控件名存在 + 三个旧名（含「对话页」）零出现，作为文案回归哨。web 全量 281/281 绿。两视口截图核对并入 #12 生产构建验证一轮做。
- 残留非源码命中：`apps/repoqa-web/dist/`（旧构建物）与 `.mimosa/hook-state` 扫描快照含旧文案，随下次构建/扫描自然滚出，不算 UI 文案。
