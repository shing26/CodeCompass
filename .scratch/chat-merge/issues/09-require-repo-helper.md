# 09：requireRepo 助手——收敛路由层 404 样板

> *This was generated by AI during triage.*

Status: closed
标签：enhancement / P3 / 来源：v0.25.0 双轴 review（Duplicated Code）

## Agent Brief

**Category:** enhancement
**Summary:** v0.25.0 批次 2 拆分时原样搬迁了 `if (!repo) res.status(404).json(...)` 同形样板（三个路由域文件共约 20 处），应收敛为共享守卫。

**Current behavior:**
所有 repo-scoped 端点在 handler 开头手工重复：按 `:id` 查仓储 → 未命中则 404 JSON。响应形状基本一致但个别文案有出入。

**Desired behavior:**
路由依赖契约（HttpDeps 所在模块）导出一个 `requireRepo` 型守卫（middleware 或显式调用皆可），所有 repo-scoped 端点复用；未命中统一返回同一 404 JSON 契约。

**Key interfaces:**
- `HttpDeps` 契约所在模块 — 新增导出 `requireRepo`（签名建议：输入 repoId + deps，返回 repo 或已发响应）
- 全部 repo-scoped 注册函数 — 改为复用守卫

**Acceptance criteria:**
- [x] 404 响应形状统一为 `{ error: string }`（现契约不变）且各端点路径/方法/成功形状零变化
- [x] e2e gate 55 项全绿（活流量证据）
- [x] 控制面全量测试绿（基线 580 起步）；若个别测试断言旧文案差异，按统一后契约收敛并保留用例覆盖
- [x] 三个路由文件内不再出现手写的 repo-缺失-404 块

**Out of scope:**
- 任何端点的新增/删除/改名；repos 仓储层接口

## Comments

- 实现（2026-09-10）：`routes/deps.ts` 新增显式调用型守卫 `requireRepo(deps, res, repoId)`——命中返回 `Repo`，未命中发出统一 `{ error: 'Repo not found' }` 404 并返回 `null`。三域 18 处同构样板（analysis 11 / repos 4 / workbench 3；grep 核实全部逐字一致，票面「约 20 处、个别文案有出入」实测无差异）替换为 `const repo = requireRepo(...); if (!repo) return;`。非 repo 守卫（Task/File/Start-symbol not found）原样保留。
- 验证：控制面 tsc 干净 + 580/580 全绿（既有测试零改断言——文案本就统一）；e2e closeout gate 55/55（含 architecture-delta 与 chat/evolve 全链活流量）。
