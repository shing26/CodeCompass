# Spec: v0.23.0 scan 信号提纯第二轮（issues 04 + 06）

> *This was generated by AI during triage.*

- 日期：2026-09-07
- 前置：v0.22.0（b55c6ff）已收口；本线 fixed point = `87e7eef`
- 来源：`.scratch/scan-dogfooding-v021/` dogfooding 取证（lazygit Go / spring-petclinic Java）

## 已定决策（grill 访谈，2026-09-07）

1. **范围**：本线保持纯 MCP 分析工具面——只做 scan 提纯；**LLM 节点池热切换整件归智能体线**（worker 会话编排/接线在那边，本线不碰 `repoqa-llm.ts` 的 provider 面）。那边立项时可引用 dogfooding 取证（本 session 402 事故）与本 spec 边界段。
2. **issue 04 策略**：flag 降权——命中白名单的符号**不进孤儿桶 items 榜单、不计入 total**，孤儿桶新增 `ScanBucket.wiredExcluded` 计数字段保留信息。（实现期修订：原设想的 `ScanCandidate.wired` 字段永不输出——wired 符号不进榜单——属 speculative generality，收窄为仅 `wiredExcluded`；`wiredKindOf` 导出在 scan-engine 供未来工具复用。）
3. **issue 06 策略**：命名 + 行数双条件降权——hubs 榜单排除「有 parentType + 名称 `^get/^set/^is` + 方法体 ≤5 行」的 accessor；PageRank 全图计算不动（只滤榜单展示），hubs total 同步排除。

## 文件面声明（并行协作约定）

| 文件 | 归属 | 本线改动 |
|---|---|---|
| `packages/contracts/src/repoqa.ts` | **共享区** | `ScanBucket` 加 optional `wiredExcluded?` 字段（additive optional，无枚举/必填变更） |
| `services/control-plane/src/scan-engine.ts` | MCP 工具面 | 白名单常量、wired 识别、孤儿桶过滤、hubs accessor 过滤 |
| `services/control-plane/src/scan-engine.test.ts` | MCP 工具面 | issue 04/06 单测 |
| `scripts/e2e/closeout_gate.py` | 共享区 | scan 相关断言按需补 wired 行为断言（工具数 17 不变，无计数 gate 改动） |
| CHANGELOG / README / CONTEXT.md / 版本六处 | 共享区 | 收口时一次性（收口人=本线） |

## 识别面（issue 04 白名单，多语言复用常量）

- 注解存储格式已核实：`RepoSymbol.annotations` 为**带 `@` 的完整注解文本数组**（如 `'@Bean'`、`'@FeignClient("customers")'`），匹配用边界正则（先例：`JavaAdapter.ts:360-362` kind 推断）。
- `wired: 'di'`：符号或其所属声明携带 `@Bean` / `@FeignClient` / `@EventListener` / `@Configuration`（di/entry 二分在实现期收窄：`wiredExcluded` 计数不区分类型，`wiredKindOf` 保留返回值供未来工具复用）
- `wired: 'entry'`：kind=method 且 name=`main`（Java/Go 通用）；class 携带 `@SpringBootApplication`
- 白名单常量 `WIRED_ANNOTATIONS`（正则数组）集中在 scan-engine.ts，供多语言复用
- hubs 桶新增披露 note（review 采纳）：accessor 从榜单与 total 排除需在 payload 里可见，否则消费方对照 PageRank 会看到无法解释的缺席

## 验收基准（对照 dogfooding 取证）

- spring-petclinic-microservices 重扫：`loadBalancedRestTemplate`/`routerFunction`（@Bean）、`CustomersServiceClient`/`VisitsServiceClient`（@FeignClient）、`*Application.main` 不再进孤儿桶候选；`wiredExcluded` 计数合理。
- hubs top10 无 `Vet.getSpecialtiesInternal`/`Visit.setPetId` 类纯 accessor；带逻辑的 `getOrCreateX`（>5 行）不误伤。
- golden eval 97 题全阈值不回退；既有孤儿/hubs 单测语义对齐新过滤规则。

## 边界

- 孤儿桶 nextAction/note 文案随过滤规则对齐（DI/入口已单列，DEPRECATE 引导不再与 note 矛盾），但不做新语义承诺。
- 方法体级 AST 提取器（backlog #1）、Go 跨文件类型引用（`declaredTypes` 按文件）不在本版。
- gate 未新增 wired 断言：scan 仅在 MCP 面暴露，demo-polyglot fixture 无 main/@Bean，为单条计数断言再开一个 stdio session 不值；wired 语义由 scan-engine 单测全覆盖（11/11）。
