# Issue 04 — 影响力兑付（v1.0.0 + 技术文 + 公开评测）

> Spec：`.scratch/v031-precision/spec.md` §3（M5）、§1（交付 D）
> 波次：Wave 3（收口批）｜ 依赖：票 02（精度数字）、票 03（发布物）

## 目标

把 v0.30–v0.31 的成熟度兑换成**外部可验证物**（M5 ≥ 3）：发布物、带真实数据的技术文、公开精度报告。

## 内容

1. **tag `v1.0.0`**：版本五处推进（四本 package.json + `version.ts`）+ CHANGELOG `[1.0.0]` 段 + README 版本行；收口人制 + 双轴 `code-review` + 全量门禁（cp 650 / web 363 / bridge 26 / e2e 63）+ Release CI 双绿。
2. **技术文**（核心资产）：主题 =「确定性 AST 图谱在真实仓库上的精度边界」。
   - 必须含真实数据：43% / 31% / 83% 的起点 → 逐项根因 → 修复后数字（票 01/02 产出）。
   - 必须含**失败面**：到不了 0 的部分与原因（这比"我做了个分析工具"有说服力，也是本项目最稀缺的诚实叙事）。
   - 素材现成：17 个 ADR、`docs/profiling.md` 的 26.3s/438MB 大规模索引基线、golden eval 97 题、V27-31 那个 WS 优雅关闭挂死的三轮破案过程。
3. **公开精度评测报告**：入 `docs/reports/`（与体验报告同级），数据来自票 01 的 harness，可复跑、可证伪。
4. **`docs/benchmark.md` 可复现段**（与票 03(c) 合流）：一条命令复现全部数字。

## 验收

- [ ] tag `v1.0.0`：CI/Release 双绿，`/health` 与 MCP 握手版本均为 1.0.0（gate 棘轮执法）
- [ ] 技术文发布（平台由用户定），含 ≥3 组真实数字与 ≥1 组失败数据
- [ ] 公开精度报告在 `docs/reports/`，任何人可照文档复跑
- [ ] M5 清单可枚举：npm 包 / 文章 / benchmark / 精度报告（≥3 项对外可见）

## 风险与时间敏感性

- **宿主平台下沉窗口**：若 Cursor / Anthropic 半年内把跨语言调用链与逐字锚点做进原生索引，叙事价值迅速衰减 → 本票宜早不宜晚。
- 文章涉及公开仓库数据（lazygit / petclinic 为开源项目）：只引用公开仓库与其文件行号，不涉及任何私有仓与配置值（ADR-0003 脱敏边界）。
