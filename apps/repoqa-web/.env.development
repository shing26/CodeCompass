# Dev server runs on 5173 while the API stays on 43110 — pin it explicitly.
# Production builds (mode=production) omit this file → same-origin /api.
# R4 (V27-1): 用 127.0.0.1 而非 localhost——控制面默认只绑 v4 回环，钉死
# IPv4 省掉浏览器每连接一次 ::1 探测往返，也免疫病态 hosts（只留 ::1 条目）。
VITE_REPOQA_API_BASE=http://127.0.0.1:43110