---
status: proposed
---

# RepoPulse 是只读、本地优先的代码智能工作台

RepoPulse 以 Multi-Harness Workbench Control Plane 为基础，承担“读代码并给出可验证证据”的职责，而不是通用 Agent。决定：对源码仓库只读，永不做写回、修改或代码生成；索引与用户事件默认只落在本地 SQLite，不上传。选择它是因为新开发者的信任来自“答案可校验、代码不会被改动”，同时避免产品滑向通用开发助手；代价是放弃实时写入、云端同步和协作能力，这些留给未来按需单独设计。
